name: Render Dignitate Carousel

on:
  repository_dispatch:
    types: [render-carousel]

jobs:
  render:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    concurrency:
      group: remotion-carousel-${{ github.event.client_payload.job.chatId || github.run_id }}
      cancel-in-progress: false

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Init debug dir
        run: |
          mkdir -p /tmp/remotion-render
          echo "run_id=${{ github.run_id }} sha=${{ github.sha }}" > /tmp/remotion-render/gha-carousel.txt

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: "npm"
          cache-dependency-path: remotion-video/package.json

      - name: Install dependencies
        working-directory: remotion-video
        run: npm install --no-audit --no-fund

      - name: Install Chrome for Remotion
        working-directory: remotion-video
        run: npx remotion browser ensure

      - name: Render carousel
        working-directory: remotion-video
        env:
          INPUT_PROPS: ${{ toJson(github.event.client_payload) }}
          SUPABASE_URL: ${{ secrets.SUPABASE_URL || secrets.NEXT_PUBLIC_SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          SUPABASE_BUCKET: ${{ secrets.SUPABASE_BUCKET || 'videos' }}
          N8N_WEBHOOK_URL: ${{ secrets.N8N_WEBHOOK_URL }}
        run: npx tsx src/render-carousel.ts

      - name: Upload artifact on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: carousel-render-debug
          path: /tmp/remotion-render/
          if-no-files-found: warn
          retention-days: 1
